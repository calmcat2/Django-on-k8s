apiVersion: v1
kind: Service
metadata:
  name: mysql-svc
  namespace: "{{- .Values.namespace}}"
  labels:
    {{- include "django-app.labels" . | nindent 4 }}
spec:
  
  ports:
  - port: 3306
    targetPort: 3306
  selector:
    app: mysql
  